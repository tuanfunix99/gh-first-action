name: First WorkFlow

on: workflow_dispatch

jobs:
 first_job:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v3
   - name: print hello
     run: echo "hello"
   - name: print gb
     run: echo "goodbye"
     
